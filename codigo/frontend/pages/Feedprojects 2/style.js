import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    alignCardProject: {
        height: '100%',
        backgroundColor: '#000',
        justifyContent: 'center',
    },
});